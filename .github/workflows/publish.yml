name: Publish to Qiita

on:
  push:
    branches:
      - master # ★ あなたのブランチ名に合わせて修正（master または main）

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        # エンコーディングの問題回避のため、Git設定を追加
        with:
          lfs: true 
          fetch-depth: 0
          submodules: true
          persist-credentials: false
          
      - name: Setup Git for LF/CRLF and encoding
        # 改行コードやファイル名エンコーディングの問題を解決
        run: |
          git config --global core.autocrlf false
          git config --global core.quotepath false
          git config --global core.editor "vim"
          git config --global core.ignorecase false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # 安定版のNode.jsバージョン

      - name: Install Qiita CLI
        # 【修正なし】ローカルインストール
        run: npm install qiita 

      - name: Publish to Qiita
        env:
          # GitHub Secretsに登録したアクセストークン名
          QIITA_TOKEN: ${{ secrets.QIITA_TOKEN }} 
        # 【💡 最終修正】ローカルパスにある実行ファイルを node で直接呼び出す
        run: node ./node_modules/qiita/lib/qiita.js publish --all